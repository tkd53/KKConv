#!/usr/bin/perl
#=====================================================================================
#                       PRF.perl
#                             bShinsuke Mori
#                             Last change 16 July 2011
#=====================================================================================

# 機  能 : 再現率と適合率とＦ値の表を生成する。
#
# 使用法 : PRF.perl (FILE)+
#
# 実  例 : PRF.perl (FILE)+
#
# 注意点 : Accuracy.perl の出力を仮定


#-------------------------------------------------------------------------------------
#                        require
#-------------------------------------------------------------------------------------

use Env;
use English;
use File::Basename;
unshift(@INC, dirname($0), "$HOME/usr/lib/perl");

require "Help.pm";
require "Parallel.pm";


#-------------------------------------------------------------------------------------
#                        check arguments
#-------------------------------------------------------------------------------------

((@ARGV > 0) && ($ARGV[0] ne "-help")) || &Help($0);

@FILE = @ARGV;


#-------------------------------------------------------------------------------------
#                        set variable
#-------------------------------------------------------------------------------------

#$INPUT_RECORD_SEPARATOR = "\n\n";


#-------------------------------------------------------------------------------------
#                        main
#-------------------------------------------------------------------------------------

foreach $FILE (@FILE){
    open(FILE) || die "Can't open $FILE: $!\n";
    @line = (<FILE>)[-4, -3, -2, -1]; 
    close(FILE);
    $P = (split(" ", $line[0]))[4];
    $R = (split(" ", $line[1]))[4];
    $F = (split(" ", $line[2]))[2];
#    printf("%s %s\n", $FILE, $P);
#    printf("%s %s\n", $FILE, $R);
    printf("%s %s\n", $FILE, $F);
}    



#-------------------------------------------------------------------------------------
#                        close
#-------------------------------------------------------------------------------------

close(FILE1);
close(FILE2);
exit(0);


#=====================================================================================
#                        END
#=====================================================================================
